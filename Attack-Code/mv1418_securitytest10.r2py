# Open a file
myfile=openfile("look.txt", True)
 
# Checks if you delete characters
try:
  myfile.writeat("A",0)
  myfile.writeat("",0) 
  myfile.writeat("a",0) 
  readVal = myfile.readat(1,0)

except ValueError:
  #If security layer successful, this should fail
  pass

else:
  #If security layer fails
  log("Secure data compromised!\n")	
 
finally:
  # Close the file after our attempt.
  myfile.close()